CREATE TABLE Products ( id INT NOT NULL AUTO_INCREMENT PRIMARY KEY, ProductName VARCHAR(50) NOT NULL, Price DOUBLE NOT NULL );
CREATE TABLE Temperatures ( dateYMD VARCHAR(50) NOT NULL, temperature FLOAT, humidity FLOAT );